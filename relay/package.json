{
  "name": "bhl-scoreboard-relay",
  "version": "1.0.0",
  "description": "WebSocket relay — WPF app pushes state, browsers subscribe",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
