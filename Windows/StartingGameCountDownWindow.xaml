<Window x:Class="Scoreboard.Windows.StartingGameCountDownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Scoreboard.ViewModels"
        xmlns:h="clr-namespace:Scoreboard.Helpers"
        mc:Ignorable="d"
        Topmost="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Icon="../Resources/appIcon.ico"
        ResizeMode="NoResize"
        Title="StartingGameCountDownWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:StartingGameCountDownViewModel />
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding StartCountDownCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border CornerRadius="16">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#0d0d1a" />
            </Style>
        </Border.Style>
        <i:Interaction.Triggers>
            <i:DataTrigger Binding="{Binding IsAlternateColor}" Value="True">
                <i:ChangePropertyAction PropertyName="Background" Value="#0d0d1a" />
            </i:DataTrigger>
            <i:DataTrigger Binding="{Binding IsAlternateColor}" Value="False">
                <i:ChangePropertyAction PropertyName="Background" Value="#1a1a2e" />
            </i:DataTrigger>
        </i:Interaction.Triggers>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Background="Transparent"
                   Foreground="White"
                   FontSize="180"
                   FontWeight="Bold"
                   FontFamily="Garamond"
                   HorizontalContentAlignment="Center">
                <Binding Path="Clock">
                    <Binding.Converter>
                        <h:TimeSpanConverter />
                    </Binding.Converter>
                </Binding>
            </Label>
        </StackPanel>
    </Border>
</Window>
