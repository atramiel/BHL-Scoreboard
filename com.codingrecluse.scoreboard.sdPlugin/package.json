{
  "name": "scoreboard-streamdeck-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --outfile=bin/plugin.js",
    "watch": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --outfile=bin/plugin.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@elgato/streamdeck": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.0.0"
  }
}
